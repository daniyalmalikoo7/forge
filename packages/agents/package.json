{
  "name": "@forge/agents",
  "private": true,
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/langgraph": "^0.2.0",
    "@langchain/anthropic": "^0.3.0",
    "@langchain/core": "^0.3.0",
    "zod": "^3.23.0",
    "@forge/schema": "workspace:*",
    "@forge/prompts": "workspace:*",
    "@forge/logger": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
